{
  "name": "ai-based-gym-equipment-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd frontend && npm run preview"
  }
}
